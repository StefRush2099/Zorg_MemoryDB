# Zorg MemoryDB v1.2.46

This maintenance release keeps the public documentation aligned with the current additive OpenClaw branch-overlay design.

## Changed

- Clarified the public changelog catch-up for recent MemoryDB design changes: DB-only durable memory, recency-aware recall weighting, deep-recall enforcement windows, vector/neural recall architecture, dynamic benchmark guidance, markdown statement import, X posting guards, and public-safe operational docs.
- Repaired the first-use guide formatting so install/access tables and command examples render as normal Markdown.
- Marked the Standard Ubuntu / existing OpenClaw git-branch overlay as the preferred current path while retaining Docker, Dockge, and Docker run notes for installs that still use those surfaces.

## Public-Safety Boundary

This release note contains documentation structure only. It does not include private DB rows, dumps, contacts, transcripts, credentials, account data, emails, internal operator context, or live benchmark result rows.

## Verification

- Queried backend DB recall before making documentation changes.
- Reviewed current README, changelog, release-process, rules/recall, schema, database-recovery, why-install, upgrade, and first-use documentation surfaces.
- Confirmed the local repository has unrelated/divergent git state that currently blocks safe tag/push/release verification from this cron run without a separate repository reconciliation step.
