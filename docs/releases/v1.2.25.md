# v1.2.25 - Documentation Folder Consistency Pass

Released: 2026-05-18

## Changed

Rechecked all markdown documentation for remaining user-facing install or upgrade folder examples that could imply the assistant should live in a repository-named or database-named subfolder.

Reworded historical changelog text that still mentioned clone/import folder behavior using the repository name.

Kept literal repository URLs, GitHub repository names, image names, backup file names, and rule-file names where they are the actual object being referenced.

## Verification

Scanned all markdown for old repository-named/database-named assistant directory patterns and hard-coded support container examples, with exact old patterns kept out of release text so they are not reintroduced as guidance.

Validated Docker Compose configuration with `docker compose --profile cli config`.
