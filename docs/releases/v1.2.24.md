# v1.2.24 - Upgrade Docs for Beginner Users

Released: 2026-05-18

## Changed

Updated upgrade documentation to use assistant-named folders such as `front-desk-assistant` and `my-ai-assistant` instead of repository/database implementation names.

Added plain-language explanations for each upgrade step in the Standard Ubuntu, Docker Compose, Dockge, and Docker run paths.

Updated support verification commands so Docker run uses the container name derived from the current assistant folder.

Replaced old changelog references that kept teaching database-named stack/project examples.

## Verification

Compared the upgrade wording against official OpenClaw updating, install, and Docker documentation.

Validated Docker Compose configuration with `docker compose --profile cli config`.

Scanned all markdown files for old database-named assistant directory examples and hard-coded support container examples, with exact patterns kept out of release text so they are not reintroduced as guidance.
