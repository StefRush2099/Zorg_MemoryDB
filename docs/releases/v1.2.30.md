# v1.2.30 - Ubuntu Upgrade Git Pull Sudo Correction

Released: 2026-05-18

## Changed

Updated Standard Ubuntu upgrade docs to refresh the Zorg MemoryDB overlay with `sudo git pull --ff-only` before verification.

Updated Dockge upgrade docs to use `sudo git pull --ff-only` for `/opt/stacks/...` stack folders that may need administrator permission.

## Verification

Scanned markdown for remaining bare `git pull --ff-only` upgrade commands in Ubuntu-owned install paths.

Re-ran markdown and Compose verification.
