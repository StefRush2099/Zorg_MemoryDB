# Zorg MemoryDB v2.0.0

This is a complete point-in-time release snapshot of the public Zorg MemoryDB
package. It is not a differential update or an overlay patch. A fresh checkout
and the attached archive contain the full published package state.

## Included snapshot

- Complete PostgreSQL-backed `zorg-db-memory` skill and DB-first safety rules.
- Full schema, stored-procedure recall APIs, semantic tooling, backup,
  recovery, install, and verification surfaces.
- Current LAN Command Chat source with live PostgreSQL gauges and the Memory 3D
  toggle.
- Connected Memory Brain 3D source and its install path.
- Public documentation, screenshots, release metadata, and package tooling.
- The v1.2.72 gauge publication and real reply-timing rule are included in the
  snapshot baseline.

## Verification

- Package version is `2.0.0` in the root metadata and archive.
- The archive is built from the complete repository package tree, not a diff.
- LAN Command Chat TypeScript compilation and production build pass.
- Memory Brain 3D JavaScript syntax checks pass.
- Public package scan, archive scan, shell checks, and diff checks pass.
- PostgreSQL/Zorg MemoryDB tables and speed checks pass.
- The GitHub tag and release asset resolve to the same full-snapshot commit.
