# Zorg MemoryDB v1.2.65

## Summary

This correction release fixes the public screenshot ordering and replaces the incorrect LAN Command Chat Memory 3D toggle captures.

## Changes

- Replaces the LAN Command Chat Memory 3D toggle screenshots with reviewed captures from the local `Zorg Rush` system.
- Corrects the desktop and mobile dark-mode toggle screenshots so dark mode is actually active.
- Removes the wrong non-local screenshot source from the public package.
- Keeps original LAN Command Chat screenshots preserved under `docs/assets/`.
- Reorders README and screenshot docs so LAN Command Chat screenshots appear first and Memory Brain 3D screenshots appear after them.

## Verification

- Backend Zorg MemoryDB recall before work.
- Visual inspection of replacement screenshots before packaging.
- `memory_sql_tool.py tables`
- `memory_speed_test.py`
- public package verification
- package archive build
- browser screenshot inspection of the GitHub main page after push
