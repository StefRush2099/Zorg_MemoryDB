# v1.2.27 - Existing Install Upgrade Documentation

Released: 2026-05-18

## Changed

Added `docs/upgrade-existing-openclaw.md` for users who already have OpenClaw installed and need to add or refresh the Zorg MemoryDB overlay.

Updated `docs/upgrading.md` so it explains the difference between OpenClaw itself, the Zorg MemoryDB overlay, assistant folders, state folders, containers, and images before listing upgrade choices.

Expanded Docker Compose, Dockge, and Docker run upgrade pages with beginner explanations of what each install type replaces and what data folder must be preserved.

Linked the existing-install path from README and quickstart documentation.

## Verification

Compared against official OpenClaw install and updating docs.

Validated Docker Compose configuration with `docker compose --profile cli config`.

Scanned markdown for placeholder paths, mixed upgrade commands, old database-named assistant folder examples, and bad one-shot Compose/TUI commands.
