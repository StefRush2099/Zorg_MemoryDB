# Zorg_MemoryDB v2.0.15

## Core approval gate and ANN recall repair

- Promotes the fact-based summary and uppercase GO-before-mutation gate to a core system rule for every system change.
- Repairs ANN recall using canonical logic-rule UUID queueing, high-weight aliases, cached regression queries, and durable query observations.
- Classifies the requested MemoryDB schedules as `core_llm`, removes personal routing/attribution, and keeps ANN executors inside the canonical skill.

## Native PostgreSQL 18 deployment documentation

- Carries forward the verified native PostgreSQL 18.4 deployment path, clean-cluster restore, acceptance checks, and LAN Command Chat version gate from v2.0.14.
