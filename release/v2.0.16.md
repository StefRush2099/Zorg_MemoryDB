# Zorg_MemoryDB v2.0.16

## Core-rule preflight pipeline

- Makes the summary and uppercase GO mutation gate the rank-1 first pipeline layer before normal logic-rule, weighted, lexical, or ANN recall.
- Adds bounded dynamic weighting, a semantic `precedes -> all_core_rules` edge, and a dedicated PostgreSQL preflight function.
- Verifies live rank order with representative regression queries while preserving all source memory.
