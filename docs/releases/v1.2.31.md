# v1.2.31 - Broader Sudo Documentation Pass

Released: 2026-05-18

## Changed

Updated Dockge terminal fallback commands to use `sudo docker compose ...` on fresh Ubuntu hosts.

Updated Dockge multiple-stack examples under `/opt/stacks` to use `sudo git clone` and `sudo chown`.

Updated Dockge cleanup examples to use `sudo docker ...`.

Added beginner guidance explaining that Docker permission errors may require `sudo docker ...` unless the user is already in the Docker group.

## Verification

Scanned markdown for bare Dockge `/opt/stacks` clone and Docker terminal examples that need `sudo`.

Re-ran markdown and Compose verification.
