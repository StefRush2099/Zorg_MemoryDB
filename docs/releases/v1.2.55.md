# v1.2.55 - Existing-problem memory recovery rule

This release records the 2026-05-30 operating-rule hardening for existing system failures without changing install behavior.

- Strengthens the exhaustive memory rule so existing jobs, settings, integrations, workflows, and prior assistant-built systems are treated as having a recoverable prior working path until backend DB memory and live state are genuinely exhausted.
- Adds a public-safe DB seed, `db/exhaustive_memory_existing_problem_rule_2026_05_30.sql`, that upserts the structured rule into `zorg_logic_rules` when applied.
- Updates core markdown/template rule surfaces so clean installs inherit the stronger DB-first recovery behavior.

No installer, package, service, routing, auth, or runtime install path changes are included.
