# Zorg MemoryDB v3.0.4

This release aligns the packaged Memory Brain 3D runtime with the documented
Neural Recall Activity surface.

- Replaces the stale user-facing `Memory Brain Status` label in the server
  identity default, HTML heading, and client fallback.
- Keeps PostgreSQL-backed recall, semantic work queues, ANN wiring, and the
  shared LAN Command Chat integration unchanged.
- Aligns the public package and LAN Command Chat package versions at 3.0.4.
