# Zorg_MemoryDB v2.0.14

## Native PostgreSQL 18 deployment documentation

- Documents the completed workspace-local PostgreSQL 18.4 clean-cluster logical restore and cutover.
- Defines the required backup, restore, acceptance, dependent-surface, and rollback gates.
- Keeps the retired PostgreSQL container and its rollback volume local and prevents accidental recreation through the retired profile policy.
- Excludes live data, database dumps, logs, credentials, SQL maps, binaries, build output, and generated caches from the public package.
- Includes the latest public-safe LAN Command Chat responsive CSS correction.
