# zorg-db-memory v1.2.67

This release corrects the exported `zorg-db-memory` skill metadata after the
GitHub posting/release hard gate was added.

## Changes

- Replaced the temporary repair-note skill description with the canonical
  `zorg-db-memory` description.
- Kept the full hard GitHub posting/release checklist in the live skill and the
  public package.
- Preserved the corrected screenshot ordering and reviewed screenshot assets
  from v1.2.65.
- Rebuilt the release archive so package metadata, release notes, and the skill
  package match.

## Verification

- Backend PostgreSQL/Zorg MemoryDB recall completed before work.
- `memory_sql_tool.py tables` passed.
- `memory_speed_test.py` passed.
- Public package verification passed.
- Archive-content checks passed.
- GitHub release page and rendered package surfaces were checked after publish.
