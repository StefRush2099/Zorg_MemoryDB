# Zorg_MemoryDB v2.0.13

## ANN/vector recall completion

- Uses `nomic-embed-text:latest` consistently across schema, procedures, router, worker, cache, and installer defaults.
- Creates the ANN model slot, canonical source queue, query cache, scheduled jobs, and upgrade functions idempotently.
- Handles legacy queue rows without aborting the worker transaction; unsupported/stale rows are isolated from supported ANN work.
- Preserves source memory and reports readiness through `memory_ann_bootstrap_status_v1`.
