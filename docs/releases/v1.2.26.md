# v1.2.26 - Separated Beginner Upgrade Pages

Released: 2026-05-18

## Changed

Replaced the single mixed upgrade runbook with a chooser page plus separate pages for Standard Ubuntu, Docker Compose, Dockge, and Docker run.

Added step-by-step command explanations to each upgrade page.

Removed placeholder upgrade paths from the beginner upgrade flow.

Updated Docker run install examples to create and enter `~/front-desk-assistant` before deriving the container name.

## Verification

Compared against official OpenClaw install, updating, and Docker documentation.

Validated Docker Compose configuration with `docker compose --profile cli config`.

Scanned markdown for placeholder paths, escaped markdown backticks, mixed upgrade links, and old database-named folder examples.
