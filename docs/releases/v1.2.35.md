# v1.2.35 - Idle Context Pruning Rule

## Added

Added the idle bootstrap and follow-up context pruning rule for DB-backed recall sessions.

First request after idle may gather broader DB-backed rule/project/operator context for safety.

Second follow-up in the same task thread must prune active context down to the smallest task-relevant subset while preserving hard rules and exact task constraints.

## Verification

Core markdown rule surfaces should include `IDLE_CONTEXT_PRUNING_RULE`.

DB structured recall should return the rule for natural-language queries about second follow-up pruning after idle.

The rule must not delete, compact, or age out source memory; it only reduces repeated context-window carryover.
