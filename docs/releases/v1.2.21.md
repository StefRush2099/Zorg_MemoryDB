# v1.2.21 - 2026-05-18

This release clarifies Docker Compose and Dockge install folder naming for non-technical operators.

## Changed

Replaced MemoryDB-flavored example folders with assistant-facing names such as `my-ai-assistant`, `front-desk-assistant`, and `research-assistant`.

Added text explaining that the final `git clone` argument is the local install folder/name customers will see when managing the assistant.

Updated Dockge multiple-install and upgrade placeholders to use assistant names instead of MemoryDB implementation names.

## Verification

Verified current install docs no longer use MemoryDB-flavored names as user-facing destination folder examples.

Verified Compose config still validates after the documentation-only change.
