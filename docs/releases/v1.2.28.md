# v1.2.28 - Beginner First-Use and LAN Chat Password Documentation

Released: 2026-05-18

## Added

Added `docs/using-zorg-memorydb.md` for post-install use: LAN command chat URLs, first password request flow, TUI fallback, manual password rotation, and Control UI access.

Added `scripts/generate_lan_chat_password.py` to generate PBKDF2-SHA256 LAN chat password hashes and cookie secrets while printing the one-time plaintext password for approved private delivery.

## Changed

Wired `LAN_CHAT_PASSWORD_HASH` and `LAN_CHAT_AUTH_SECRET` through Docker Compose and native LAN chat service env files.

Linked the first-use guide from README, quickstart, install, Dockge, Docker, and LAN console docs.

## Verification

Compared OpenClaw Control UI, Gateway, TUI, and install behavior against official OpenClaw documentation before editing.

Validated the password generator output format.

Validated Docker Compose configuration with `docker compose --profile cli config`.
