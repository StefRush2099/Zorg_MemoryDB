# Zorg MemoryDB v4.0.1

- Removes the retired `package/zorg/memory-3d/` package.
- Adds the captured production browser assets for the separate Neural Recall
  Activity service running on port 8097.
- Documents what an OpenClaw agent must install, what it must remove, and how
  to verify the live PostgreSQL-backed service.
- Aligns the root, LAN Command Chat, and `zorg-memorydb` package versions.
