# v1.2.22 - 2026-05-18

This release finishes the Dockge naming pass so first-time users see assistant-facing stack names throughout the active setup docs.

## Changed

Updated the base setup architecture diagram to use `front-desk-assistant`.

Updated Dockge stack naming principles to prefer customer-readable assistant names.

Updated the example Dockge stack `name:` to `front-desk-assistant`.

Tightened Dockge install notes so stack folders are named after the assistant, not the MemoryDB implementation.

## Verification

Searched active docs for confusing MemoryDB-flavored Dockge stack examples.

Verified remaining `Zorg_MemoryDB` references in active docs are repository/script paths or rule file names, not customer-facing Dockge stack examples.
