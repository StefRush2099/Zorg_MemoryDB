# v1.2.29 - Absolute Beginner Terminal and SSH Documentation

Released: 2026-05-18

## Added

Added `docs/beginner-terminal-and-ssh.md` explaining terminals, SSH, paths, `cd`, `pwd`, `ls`, `sudo`, `git clone`, Docker Compose, and safe handling of state folders.

## Changed

Linked the beginner terminal/SSH guide from README, quickstart, install, and upgrade docs.

Split several install command blocks into command-by-command sections with plain-language explanations between commands.

Removed duplicated internal rule appendices from public markdown pages.

Deleted the redundant sanitized-template policy page.

Replaced install-page upgrade command blocks with links to the dedicated step-by-step upgrade pages.

## Verification

Compared OpenClaw install/TUI behavior against official OpenClaw docs.

Checked Ubuntu SSH behavior against Ubuntu SSH documentation.

Scanned markdown for old bad TUI commands, placeholder install paths, and escaped shell variables.
