# Zorg MemoryDB v1.2.64

## Summary

This correction release restores screenshots directly on the GitHub main README page.

## Changes

- Adds visible screenshot embeds to `README.md` so the repository landing page shows screenshots again.
- Keeps the original LAN Command Chat screenshots visible from `docs/assets/`.
- Keeps Memory Brain 3D screenshots visible from `docs/screenshots/`.
- Keeps LAN Command Chat Memory 3D toggle screenshots visible from `docs/screenshots/`.
- Preserves the v1.2.63 skill metadata and supporting-services corrections.

## Verification

- `memory_sql_tool.py tables`
- `memory_speed_test.py`
- repository secret/generated artifact scan
- package archive build
- browser screenshot inspection of the GitHub main page showing rendered README screenshots
