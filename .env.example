# Copy to .env before production use on latest Ubuntu.
# Change DB_PASSWORD and OPENCLAW_GATEWAY_TOKEN before exposing the stack.
# Keep this lowercase so Docker Compose/Dockge do not create a second normalized stack folder.
COMPOSE_PROJECT_NAME=zorg_memorydb
DB_NAME=openclaw_memory
DB_USER=openclaw_memory
DB_PASSWORD=openclaw_memory
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_GATEWAY_BIND=lan
OPENCLAW_GATEWAY_AUTH=token
OPENCLAW_GATEWAY_TOKEN=change-me-zorg-token
# Docker build argument; leave as latest unless pinning a known-good OpenClaw release.
OPENCLAW_VERSION=latest
