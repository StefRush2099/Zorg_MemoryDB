# Zorg MemoryDB v1.2.62

## Summary

This correction release keeps the v1.2.61 focused-package structure and fixes the screenshot/package handling around LAN Command Chat and supporting services.

## Changes

- Preserves existing LAN Command Chat screenshots under `docs/assets/`.
- Documents those preserved screenshots in `docs/screenshots.md` instead of treating new Memory Brain 3D screenshots as replacements.
- Keeps the newer curated LAN Command Chat and Memory Brain 3D screenshots under `docs/screenshots/`.
- Adds the `zorg-db-memory` supporting-services reference to the packaged skill.
- Documents discovery-first behavior for `cloudflared`, ComfyUI / `comfyui-nvidia`, `kokoro-fastapi-cpu`, `bluenviron/mediamtx:latest`, `ollama/ollama:latest`, SearXNG, and `fedirz/faster-whisper-server`.
- Keeps missing-service installs approval-gated and Dockge-managed where possible.
- Removes generated Python cache artifacts from the public package tree and release archive.
- Tightens public package verification so Python cache artifacts fail future checks.

## Public Package Boundary

OpenClaw remains the base install. This repository carries the `zorg-db-memory` skill package and public-safe supporting code only.

The public package still excludes maintainer-only scheduled publishing instructions so installed agents do not inherit repository publishing behavior.

## Verification

- `memory_sql_tool.py tables`
- `memory_speed_test.py`
- repository secret/generated artifact scan
- package archive build
- GitHub screenshot verification for the screenshot tree and rendered screenshot docs
