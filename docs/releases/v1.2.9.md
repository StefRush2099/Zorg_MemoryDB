# Zorg MemoryDB v1.2.9

## Summary

This release performs documentation and release-line maintenance so the public repository accurately reflects the current MemoryDB design and the recent Docker/Dockge install releases.

## Changed

- Restored `CHANGELOG.md` release structure so the host-port auto-selection work is recorded under `v1.2.8` instead of remaining in `Unreleased`.
- Removed stale duplicate unreleased notes from the bottom of the changelog after they had already been published into the relevant public-safe docs.
- Preserved the public-safe documentation boundary: structure, install behavior, rules, schema summaries, and release process only.

## Verification

- Confirmed `README.md`, release process docs, documentation-maintenance docs, rules/recall docs, schema summary, recovery docs, install docs, and `docs/releases/` describe the current DB-only MemoryDB design and recent Docker/Dockge behavior.
- Confirmed the working tree contains no private database rows, dumps, contacts, credentials, emails, transcripts, or operator context for this maintenance release.

## Privacy

This release publishes only public-safe documentation maintenance. It does not include private memory rows, database dumps, contacts, emails, transcripts, credentials, live account data, internal hosts, or operator context.
