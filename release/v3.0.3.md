# Zorg MemoryDB v3.0.3

This release fixes the export/install gap that left the eleven typed runtime
capture tables without their semantic/ANN trigger bridge after installation.
The installer now applies `memory_semantic_capture_triggers_2026_07_17.sql`
and verifies all eleven triggers. Documentation also records the query-cache
prerequisite for ANN recall of uncached text.
