# v1.2.33 - Release Notes LAN Port Correction

## Changed

Corrected the GitHub Release workflow's generated Docker-run command so release notes include LAN command chat publication for the same OpenClaw/Zorg container. This was superseded in v1.2.34 by the external host-port range `-p 8080-8180:3001`.

Corrected the base setup Dockge stack example so it publishes the built-in LAN command chat port from the same Docker Compose service/container named `openclaw`.

## Verification

Scanned Docker-run examples for Gateway port publication without LAN chat port publication.

Compose config still lists only `openclaw` by default, with `openclaw-cli` only under the `cli` profile.
